## 2P-Kt on Java example

### How to reproduce

On Windows:
```bash
.\gradlew run # optionally: -Pfile=/path/to/theory/file
```

On Linux or MacOS:
```bash
./gradlew run # optionally: -Pfile=/path/to/theory/file
```
